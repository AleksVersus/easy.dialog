! 6 строка локации dialog.replic.int
! данный кусок кода исключён из работы, поскольку теперь настройки
! проставляются всем репликам во время генерации
if $args['sets']<>'':
	$sets_replic[arrpos('$id_replic',$args[0])]=$args['sets']
end
! 41 строка
! передача настроек, если не переданы до сих пор
if $args['sets']<>'' and $sets_replic[arrpos('$id_replic',$rbTEMP_ID[args['i']])]='':
	$sets_replic[arrpos('$id_replic',$rbTEMP_ID[args['i']])]=$args['sets']
end

